import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    navContainer: {
        flex: 1,
        position: 'absolute',
        bottom: 0,
        height: 80,
        flexDirection: 'row',
        alignItems: 'center',
    },
});
export default styles;