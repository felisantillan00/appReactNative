import { StyleSheet } from 'react-native';
// Estilos del text
const styles = StyleSheet.create({
    text: {
        fontWeight: 'bold',
        fontSize: 26,
        textAlign: 'center',
    },
});
export default styles;
