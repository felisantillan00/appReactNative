import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    ratingContainer: {
        flexDirection: 'row',
        alignItems: 'center',
    },
    ratingText: {
        marginLeft: 10,
        fontSize: 16,
    },
    submittedMessage: {
        color: 'green',
        marginTop: 5,
    },
});
export default styles;