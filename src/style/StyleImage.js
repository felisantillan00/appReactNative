import { StyleSheet} from 'react-native';
const styles = StyleSheet.create({
    image: {
        width: '100%',
        height: 50,
        borderRadius: 24,
        marginBottom: 10,
    },
});
export default styles;