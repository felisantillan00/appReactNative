import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    toggleButton: {
        padding: 10,
    },
    icon: {
        width: 24,
        height: 24,
    },
});
export default styles;