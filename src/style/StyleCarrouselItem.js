import { StyleSheet } from 'react-native';
const styles = StyleSheet.create({
    card: {
        marginHorizontal: 10,
        padding: 10,
        borderRadius: 34,
        backgroundColor: '#fff',
        alignItems: 'center',
    },
});
export default styles;